export type ActionType = {type: ''}
const musicPage = {}
export const musicReducer =(state: {} = musicPage, action: ActionType): {}=>{
    switch (action.type){

        default :
            return state
    }
}