
export type ActionType = {type: ''}
const newsPage = {}
export const newsReducer =(state: {} = newsPage, action: ActionType):{} =>{
    switch (action.type){

        default :
            return state
    }
}