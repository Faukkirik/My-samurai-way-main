
export type ActionType = {type: ''}
const settingsPage = {}
export const settingsReducer =(state: {} = settingsPage, action: ActionType):{} =>{
    switch (action.type){

        default :
            return state
    }
}